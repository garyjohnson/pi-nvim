{
  "watchPatterns": [
    "lua/**/*",
    "plugin/**/*",
    "tests/**/*",
    "Makefile",
    "README.md"
  ],
  "excludePatterns": [
    "**/.git",
    "**/.pi",
    "**/.firecrawl"
  ],
  "notifyWithoutConfig": true,
  "checksFast": [
    {
      "type": "script",
      "path": "scripts/test_changed",
      "fileFilter": ["tests/*_spec.lua"],
      "failurePrompt": "Fix the test failures identified above and continue working."
    }
  ],
  "checks": [
    {
      "type": "script",
      "path": "scripts/check",
      "failurePrompt": "Fix the test failures and continue working."
    },
    {
      "type": "prompt",
      "content": "Review changes made in this session. If any added behavior lacks test coverage, add test coverage (unit and e2e). If there were any critical security issues introduced to production code, address those issues."
    }
  ],
  "release": {
    "type": "prompt",
    "content": "Commit changes and push."
  }
}
